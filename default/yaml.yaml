# -----------------------------------------------------------------------------
# YAML
# -----------------------------------------------------------------------------
# Parent match
- scope: punctuation.definition.block.sequence.item.yaml, punctuation.separator.key-value.mapping.yaml
  settings:
    foreground: _base-gray
